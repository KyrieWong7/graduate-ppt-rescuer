---
description: 检测到 PPT/汇报/slides/presentation 任务时，强制进入 plan 模式收集需求后再执行。
alwaysApply: false
---

# PPT 需求摄入规则

## 触发条件

当用户的请求涉及以下任何关键词时，本规则激活：
- 汇报、PPT、slides、presentation、幻灯片、beamer、讲稿、做个PPT

## 强制执行步骤

**不得直接开始写代码或生成文件。** 必须按以下顺序执行：

### Step 1 — 切换到 Plan 模式

立即切换到 Plan 模式（调用 SwitchMode 工具）。

### Step 2 — 读取背景资料

先读取：
- `MEMORY.md`（关键技术经验）
- `WORKFLOW.md`（9 步工作流）

### Step 3 — 用 AskQuestion 收集需求

必须用 `AskQuestion` 工具收集以下信息（可合并为 2-3 个问题）：

**问题一：内容与素材**
- 汇报主题是什么？（论文章节 / 课程内容 / 独立选题）
- 有哪些现有素材？（PDF / DOCX / 笔记 / 暂无）

**问题二：格式要求**
- 目标页数和汇报时长？（如：16 页 / 8 分钟）
- 是否需要中英双语？
- 是否需要叙事主线？（如：侦探故事 / 案例研究 / 直述）

**问题三：输出配置**
- 是否需要 AI 生成配图？（需要 / 用户提供 / 不需要）
- 是否需要完整讲稿（script.pdf）？

### Step 4 — 生成需求摘要

将用户回答汇总为如下格式，保存到 `quality_reports/specs/[slug]-spec.md`：

```markdown
# 汇报需求摘要

## 主题
- [主题描述]

## 素材
- [素材列表]

## 格式要求
- 页数：[N] 页
- 时长：[N] 分钟
- 双语：是/否
- 叙事主线：[描述或无]

## 输出配置
- 配图：AI生成 / 用户提供 / 不需要
- 讲稿：是/否

## 人工确认节点
- [ ] 幻灯片结构草案
- [ ] 叙事主线确认（如有）
```

### Step 5 — 生成执行计划

调用 `CreatePlan` 工具，展示完整执行计划，**等待用户确认后再开始执行**。

## 执行阶段

用户确认计划后，调用 `ppt-pipeline` skill 执行完整的 8 阶段流水线。
